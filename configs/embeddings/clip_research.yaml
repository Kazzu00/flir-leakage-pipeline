# PLANNED research candidate, not an executed experiment or final model choice.
# Set a full HF commit SHA before the final run; unresolved revisions are refused.
# Batch size 8 is a starting value to validate on the target hardware.
extractor: clip
model_revision: null
require_resolved_revision: true
model_id: openai/clip-vit-large-patch14
feature_type: image_embedding
device: auto
batch_size: 8
dtype: float32
mixed_precision: false
store_raw: true
store_l2_normalized: true
